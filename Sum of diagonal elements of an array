#include<stdio.h>
int main ()
{
int a[3][3], i=0, j=0, sum=0;
int *p = &a[i][j];

for(i=0;i<3;i++)
{
for(j=0;j<3;j++)
{
printf("Enter the numbers : ", a);
scanf("%d", &a[i][j]);
if(i==j)
{
sum=sum+a[i][j] ;
}
}
}
printf("Sum is %d" ,sum);

return 0;
}
